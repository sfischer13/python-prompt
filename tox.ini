[tox]
minversion = 2.1
envlist = py32, py33, py34, py35, pypy3, coverage

[testenv]
deps = pytest
commands = py.test

[testenv:coverage]
basepython = python3.5
passenv = TRAVIS TRAVIS_JOB_ID TRAVIS_BRANCH
deps =
    coveralls
    pytest
    pytest-runner
commands =
    coverage run --source=src/prompt setup.py test
    coveralls
